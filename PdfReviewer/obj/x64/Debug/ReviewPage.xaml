<Page x:ConnectionId='1'
    x:Class="PdfReviewer.ReviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PdfReviewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar IsOpen="True" IsSticky="True" RequestedTheme="Dark" Background="Black">
            <AppBarButton x:ConnectionId='2' Icon="Cancel" Label="Cancel"                      />
            <AppBarButton x:ConnectionId='3' Icon="Undo" Label="Undo"                    />
            <AppBarButton x:ConnectionId='4' Icon="Save" Label="Save"                    />
        </CommandBar>
    </Page.BottomAppBar>
    <Grid x:ConnectionId='5' x:Name="mainGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock x:ConnectionId='6' Margin="10,0" x:Name="titleLabel" FontWeight="Bold" Text="Annotations"/>
        <TextBlock x:ConnectionId='7' Margin="10,0" x:Name="subtitleLabel" Grid.Row="1" Text="Use the Ink Canvas to make annotations to your document. Tap 'Save' when complete."/>

        <Border Grid.Row="2" BorderBrush="DarkGray" BorderThickness="1" Margin="10">
            <Grid>
                <Image Source="{Binding PageImage}" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                <InkCanvas x:ConnectionId='8' x:Name="inkCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                <InkToolbar x:ConnectionId='9' VerticalAlignment="Top"                                     />
            </Grid>
        </Border>
        
    </Grid>
</Page>

